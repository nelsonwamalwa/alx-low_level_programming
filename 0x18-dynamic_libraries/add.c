#include <stdio.h>

/**
 * add -The functions that finds the sum of two numbers
 * Return -  sum
 * @a - number to be added
 * @b - number to be added
*/

int add(int a, int b)
{
    int sum;

    sum = a + b;
    return (sum);

}

#include <stdio.h>

/**
 * div -This  divides tow numbers
 * @a - tHe num to be divided
 * @b - A number to be divided
 * Return - the value
*/

int div (int a, int b)
{
    int div;

    div = a / b;
    return (div);
}
